<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hospital.his.registration.persistence.mapper.RegistrationMapper">
    <sql id="summaryColumns">
        id, case_number, real_name, visit_date, department_id, employee_id,
        visit_state, regist_money AS registration_fee
    </sql>

    <select id="findById" resultType="com.hospital.his.registration.persistence.model.RegistrationRow">
        SELECT <include refid="summaryColumns"/> FROM `register` WHERE id = #{id}
    </select>

    <select id="findByCaseNumber" resultType="com.hospital.his.registration.persistence.model.RegistrationRow">
        SELECT <include refid="summaryColumns"/> FROM `register`
        WHERE case_number = #{caseNumber}
        ORDER BY visit_date DESC
        LIMIT #{limit}
    </select>

    <update id="transitionState">
        UPDATE `register` SET visit_state = #{targetState}
        WHERE id = #{id} AND visit_state = #{expectedState}
    </update>
</mapper>
